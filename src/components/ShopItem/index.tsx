import { Container } from './styles';
import { VehiclesDataInterface } from '../../models/Vehicle';

export const ShopItem = () => {
    return (
        <Container>
            teste
        </Container>
    );
}